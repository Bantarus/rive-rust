M2b Step 1 — watermark correctness (native RTX 4090, driver 591.86, release).
Frozen pose (RIVE_SPEED=0), transparent clear (RIVE_CLEAR_ALPHA=0 → genuine a<1: AA edges + glow halo),
60-frame capture warm-up. DoD: byte-identical to the proven blocking (M1b) path under BOTH the default
Fifo present mode AND a non-Fifo present mode (Immediate, where the CPU outruns the GPU).
Raw run logs: /tmp/m2b_A_block.log, /tmp/m2b_B_wm_fifo.log, /tmp/m2b_C_wm_imm.log.
===============================================================================
A = blocking M1b reference (RIVE_BLOCKING=1)        sync = blocking (M1b submit+fence)
B = M2b watermark, default Fifo                     sync = timeline-semaphore watermark (M2b, exact)
C = M2b watermark, Immediate (non-Fifo)             sync = timeline-semaphore watermark (M2b, exact)
-------------------------------------------------------------------------------
58c894e0cd22c33553c2626d4baa5cb9  cap_m2b_A_block.png
58c894e0cd22c33553c2626d4baa5cb9  cap_m2b_B_wm_fifo.png
58c894e0cd22c33553c2626d4baa5cb9  cap_m2b_C_wm_imm.png
-------------------------------------------------------------------------------
RESULT: identical md5 across A=B=C (90345 bytes) ⇒ the exact-watermark non-blocking path is pixel-identical
to the blocking reference, under both Fifo and Immediate. The 90345-byte size matches the M2a transparent
reference (cap_m2a_*_tr.png), so M2b == M2a == M1b on this content. Octopus + semi-transparent glow
visually confirmed in cap_m2b_C_wm_imm.png (the Immediate / CPU-run-ahead case).
